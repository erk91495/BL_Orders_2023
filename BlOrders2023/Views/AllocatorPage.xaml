<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BlOrders2023.Views.AllocatorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlOrders2023.Views"
    xmlns:models ="using:BlOrders2023.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sf="using:Syncfusion.UI.Xaml.DataGrid"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="AllocatorPage_Loaded">

    <Grid>
        <sf:SfDataGrid x:Name="dataGrid" 
                           ItemsSource="{x:Bind ViewModel.AllocatedOrders, Mode=TwoWay}"
                           AutoGenerateColumns="False"
                           SourceType="models:Order">
            <sf:SfDataGrid.Columns>
                <sf:GridNumericColumn MappingName="OrderID" IsReadOnly="True"/>
            </sf:SfDataGrid.Columns>

            <sf:SfDataGrid.DetailsViewDefinition>
                <sf:GridViewDefinition RelationalColumn="Items">
                    <sf:GridViewDefinition.DataGrid>
                        <sf:SfDataGrid x:Name="Items" AutoGenerateColumns="False">
                            <sf:SfDataGrid.Columns>
                                <sf:GridNumericColumn MappingName="ProductID" IsReadOnly="True"/>
                                <sf:GridNumericColumn MappingName="Quantity" IsReadOnly="True"/>
                                <sf:GridNumericColumn MappingName="QuanAllocated" IsReadOnly="True"/>
                            </sf:SfDataGrid.Columns>
                        </sf:SfDataGrid>
                    </sf:GridViewDefinition.DataGrid>
                </sf:GridViewDefinition>
            </sf:SfDataGrid.DetailsViewDefinition>
            
        </sf:SfDataGrid>
    </Grid>
</Page>
