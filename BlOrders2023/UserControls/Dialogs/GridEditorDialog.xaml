<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="BlOrders2023.UserControls.GridEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlOrders2023.UserControls.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sf="using:Syncfusion.UI.Xaml.DataGrid"
    mc:Ignorable="d"
    PrimaryButtonText="Save"
    PrimaryButtonClick="ViewModel.Save"
    IsPrimaryButtonEnabled="{x:Bind ViewModel.CanSave, Mode=OneWay}"
    CloseButtonText="Cancel">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">1200</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">1000</x:Double>
    </ContentDialog.Resources>
    <Grid>
        <sf:SfDataGrid x:Name="DataGrid"
                       ItemsSource="{x:Bind ViewModel.Items, Mode=TwoWay}"
                       CurrentCellValidating="ViewModel.ValidateItems"
                       AutoGenerateColumns="False"
                       DataValidationMode="InView"
                       EditorSelectionBehavior="MoveLast"
                       EditTrigger="OnTap"
                       AllowEditing="True">
        </sf:SfDataGrid>
    </Grid>
</ContentDialog>
